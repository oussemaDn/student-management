environment {
        // IMPORTANT: Replace the placeholder with the ID you just created!
        DOCKER_HUB_CREDENTIALS = 'dockerhub-credentials-id' 
        IMAGE_NAME = 'oussema2025/student-management'
        IMAGE_TAG = 'latest'
        SONAR_TOKEN_ID = 'sonarqube-analysis-tokenStudent-Management-App' // The Jenkins credential ID
        PROJECT_KEY = 'student-management-app'      // Your SonarQube project key
        WSL_IP = '<YOUR_CURRENT_WSL_IP>'            // e.g., '172.25.1.10'

    }
